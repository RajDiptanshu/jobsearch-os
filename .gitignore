# Dependencies
node_modules/

# Secrets — never commit
.env
.env.*
!.env.example

# Personal data / PII — resume, comp, contact details
data/profile.json
data/resume_current.md
data/cv_versions.json

# Live scraped job data & run history — real (possibly copyrighted) scraped content
data/jobs.json
data/runs.json

# Real sent artifacts — actual email digests / application drafts
data/outbox/
data/applications/
data/incoming/
!data/incoming/.gitkeep

# Logs
data/logs/

# Playwright browser binaries (installed via `npm run setup-naukri`)
.playwright-mcp/

# Third-party / personal reference material dropped into the repo dir (never publish)
*.pdf
S Product Framework.pdf

# OS/editor cruft
.DS_Store
Thumbs.db
*.log
